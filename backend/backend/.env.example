# Required — app will refuse to boot if missing
DATABASE_URL=postgresql://user:pass@localhost:5432/knownetx
JWT_SECRET=replace-this-with-a-32-char-minimum-random-string
CLAUDE_API_KEY=sk-ant-api03-...

# Optional
SCHOLARSHIP_SOURCES=https://example.com/rss-feed,https://example2.com/feed
ENVIRONMENT=production
DEBUG=false
